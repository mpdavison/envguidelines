% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-client.R
\name{calculate_guidelines}
\alias{calculate_guidelines}
\title{Calculate guidelines for a parameter}
\usage{
calculate_guidelines(
  parameter,
  media,
  context = NULL,
  target_unit = NULL,
  api_key = NULL
)
}
\arguments{
\item{parameter}{Chemical parameter name (e.g., "Aluminum", "Copper").}

\item{media}{Media type (e.g., "surface_water", "soil", "sediment").}

\item{context}{Named list of environmental parameters as strings with units.
For water: pH ("7.0 1"), hardness ("100 mg/L"), temperature ("20 °C"), chloride ("50 mg/L").
For soil: pH ("6.5 1"), organic_matter ("3.5 \%"), cation_exchange_capacity ("15 meq/100g").}

\item{target_unit}{Optional unit to convert result to (e.g., "mg/L", "μg/L").}

\item{api_key}{Optional API key. If NULL, will use GUIDELINELY_API_KEY environment variable.}
}
\value{
A list containing results, context, and total_count.
}
\description{
Calculate guideline values for a specific parameter in a given media type
with environmental context.
}
\examples{
\dontrun{
# Set API key
Sys.setenv(GUIDELINELY_API_KEY = "your_api_key_here")

# Calculate aluminum in surface water
result <- calculate_guidelines(
  parameter = "Aluminum",
  media = "surface_water",
  context = list(pH = "7.0 1", hardness = "100 mg/L")
)

# Access results as tibble
library(tibble)
as_tibble(result$results)
}
}
